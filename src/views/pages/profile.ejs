<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./style/style.css">
    <title>Profile</title>
</head>
<body>
    <%- include('../partials/layout') %>
    <% if (profile) { %>
        <div class="container-profile">
            <h1>DATOS DEL USUARIO</h1>
            <div>
                <img src=<%= profile.image %>  class="img-profile" alt="">
            </div>
            <div>
                <h2>Nombre: <%=profile.first_name%> </h2>
                <h3>apellido: <%=profile.last_name%> </h3>
                <p>Correo: <%=profile.email%> </p>
                <p>orden de carrito: <%=profile.cart_ID%> </p>
            </div>
        </div>
    <% }%>

    <% if (!profile) { %>
        <div>
            <h3>Debes iniciar session para ver tu perfil</h3>
        </div>
    <% }%>

</body>
</html>